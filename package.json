{
    "name": "zarms",
    "version": "1.0.0",
    "scripts": {
        "deploy:dev": "cd src && cp .clasp-dev.json .clasp.json && clasp push",
        "deploy:prod": "cd src && cp .clasp-prod.json .clasp.json && clasp push",
        "watch:dev": "cd src && cp .clasp-dev.json .clasp.json && clasp push --watch"
    },
    "devDependencies": {
        "@google/clasp": "^2.4.2"
    }
}