{
    "name": "zarms-db",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "deploy:dev": "cp .clasp-dev.json .clasp.json && clasp push",
        "deploy:prod": "cp .clasp-prod.json .clasp.json && clasp push",
        "watch:dev": "cp .clasp-dev.json .clasp.json && clasp push --watch"
    }
}