import BrowserLogger from './logger/browser';
/**
 * Sets the logger instance. For internal use only.
 */
export declare function setInstance(newInstance: BrowserLogger): void;
/**
 * Returns the current logger instance.
 * @deprecated
 *
 * In TypeScript, use default import:
 * ```
 *  import Logger from './Logger';
 *  Logger().info(...)
 * ```
 *
 * In JavaScript, use:
 * ```
 *  const Logger = require('./Logger').default;
 *  Logger().info(...)
 * ```
 */
export declare function getInstance(): BrowserLogger;
declare const _default: () => BrowserLogger;
export default _default;
