# temp sandbox

membersタブのテーブル描画をTabulatorで検証するためのローカル用サンドボックスです。

## ファイル
- `index.html`: Beer CSS + Tabulator を読み込むエントリ
- `main.js`: Tabulator描画ロジック
- `styles.css`: 崩れ確認しやすくするための最小スタイル

## 使い方
1. `temp/index.html` をブラウザで開く
2. `fitColumns` / `行のストライプ表示` を切り替えてUI崩れを確認する
3. `再描画` ボタンで再レンダリングを確認する