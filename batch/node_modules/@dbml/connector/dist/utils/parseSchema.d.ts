export declare const parseSchema: (schemas: string | undefined) => string[];
export declare const parseJdbcSchema: (connectionString: string) => {
    connectionString: string;
    schemas: string[];
};
export declare const parseConnectionString: (connectionString: string, connectionStringType: "jdbc" | "odbc") => {
    connectionString: string;
    schemas: string[];
};
export declare const buildSchemaQuery: (columnName: string, schemas: string[], prefix?: string) => string;
